# Core dependencies for training/inference
# Install PyTorch separately if you need a specific CUDA build:
#   https://pytorch.org/get-started/locally/
torch>=2.1
monai>=1.5.0
numpy>=1.23
nibabel>=5.2
tqdm>=4.66
matplotlib>=3.7
timm>=0.9
